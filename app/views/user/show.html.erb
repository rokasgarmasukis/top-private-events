<% if @events.length == 0 %>
  No events yet
<% else %>
  My created events:
  <ul>
    <% @events.each do |event| %>
      <li><%= event.title %>: <%= link_to "Edit", edit_event_path(event) %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @event, url: events_path do |f| %>
  <div class="mb-4">
    <%= f.label :title, class: "form-label" %>
    <%= f.text_field :title, class: "form-control", placeholder: "What's the events name?" %>
  </div>
  <div class="mb-4">
    <%= f.label :content, class: "form-label" %>
    <%= f.text_field :content, class: "form-control", placeholder: "Location? Dress code? etc." %>
  </div>
  <div class="mb-4">
    <%= f.label :datetime, class: "form-label" %>
    <%= f.datetime_field :datetime, class: "form-control" %>
  </div>
  <div class="mb-4">
    <%= f.submit "Create Event", class: "btn btn-primary" %>
  </div>
<% end %>
